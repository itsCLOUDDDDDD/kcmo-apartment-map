# Generated apartment map renderer

Built from `src/map-runtime/` in this repository. The JS, CSS, worker, license and manifest files belong together. See the repository's `docs/BUILD.md` for preview and release instructions, and `mapcn/README.md` for the established browser event contract.

This build does not retrieve, edit or export the workbook. It does not publish. Data and source updates must be validated separately before deployment.
